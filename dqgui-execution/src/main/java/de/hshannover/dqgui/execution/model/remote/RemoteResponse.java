package de.hshannover.dqgui.execution.model.remote;

/**
 * All responses can either be the requested response or an error.<br>
 * This common interface allows to specify one type and then decide via the instanceof operator.
 * 
 * @author myntt
 *
 */
public interface RemoteResponse {}
